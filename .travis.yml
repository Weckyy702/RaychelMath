dist: bionic
sudo: false

language: cpp
# compiler:
#   - clang
#   - gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - clang-12

sript:
  - CXX=/usr/bin/clang-12 CC=/usr/bin/clang-12 cmake .
  - cmake --build . -- -j 2
